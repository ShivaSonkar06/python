# 🧠 Personal Data Collector

> ✨ A simple and interactive Python project that collects personal information, displays Python data types and object IDs, and estimates the user's birth year.

## 🚀 Project Overview

**Personal Data Collector** is a beginner-friendly Python program designed to demonstrate:

- 🧑 User input with `input()`
- 🔢 Type conversion using `int()` and `float()`
- 🧩 Python data types
- 🆔 Object identity using `id()`
- 📅 Basic arithmetic for approximate birth-year calculation
- 💬 Interactive console output

## ✨ Features

| Feature | Description |
|---|---|
| 🧑 Name | Collects the user's name |
| 🎂 Age | Takes age as an integer |
| 📏 Height | Takes height in meters |
| 🔢 Favorite Number | Stores a favorite number |
| 🧠 Type Display | Shows the type of each value |
| 🆔 ID Display | Shows the Python object ID |
| 📅 Birth Year | Calculates an approximate birth year |

## 🛠️ Technologies Used

- 🐍 **Python 3**
- 💻 **Command Line / Terminal**
- 📚 Python built-in functions: `input()`, `int()`, `float()`, `type()`, `id()`

## 📸 Screenshots

### 🖥️ Program Preview

> Add your actual terminal screenshot here:

```markdown
![Program Screenshot]


```

### 📂 Recommended Project Structure

```text
fundmental-booster/
│
├── fundmental booster(2).py
├── README.md
└── screenshots/
    └── output.png
```


python "fundmental booster(2).py"
```

### 4️⃣ Enter your information

The program asks for:

```text
Name
Age
Height in meters
Favorite number
```

It then displays the collected values along with their types and object IDs.

## 📋 Example Output

```text
welcom the interactive personal data collector

please enter your name : Shiva
please enter your age : 20
please enter your height in meters : 1.70
please enter your favorite number : 7

thank you! here is the information we collected :

name: Shiva <class 'str'> ...
age: 20 <class 'int'> ...
height: 1.7 <class 'float'> ...
number: 7 <class 'int'> ...

your birth year is approximately: 2006
thanks for using the personal data collector,goodbye!
```

## 🧩 Concepts Demonstrated

### 🔹 User Input
The project uses `input()` to interact with the user.

### 🔹 Type Conversion
- `int()` converts input into integers.
- `float()` converts input into decimal numbers.

### 🔹 Python Type & Identity
The program uses `type()` to identify the data type and `id()` to display the object's identity.

### 🔹 Basic Calculation
The approximate birth year is calculated from the age using the current year represented in the program.

## 🎯 Learning Goals

This project is useful for beginners learning Python fundamentals and provides hands-on practice with:

- Variables
- User input
- Data types
- Type conversion
- Built-in functions
- Arithmetic operations
- Console-based applications

## 👨‍💻 Author

**Shiva Sonkar**

⭐ If you find this project useful, consider giving it a star!
